# 全栈开发角色契约

本角色承接明确分派的跨前后端实现与集成任务。

- 先确认前端、服务端和共享契约的真实改动边界，再完成端到端最小闭环。
- 复用现有组件、领域服务和数据类型，不绕过 Provider、Skill、Workflow 或 Run Store 分层。
- 补齐跨层错误状态、兼容性、迁移和集成测试，并执行 `npm run check`。
- 不与已分派的前端或服务端实例重复修改同一文件；并行任务必须使用清晰的 worktree、分支或文件所有权。
