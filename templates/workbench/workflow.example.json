{
  "id": "research-review",
  "description": "Run the local researcher as a one-node reusable Workflow.",
  "nodes": [
    {
      "id": "research",
      "employeeId": "local-researcher",
      "needs": [],
      "with": {}
    }
  ],
  "maxConcurrency": 1,
  "failFast": true,
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["message"],
    "properties": { "message": { "type": "string" } }
  }
}
